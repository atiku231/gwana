import React from 'react';
import type { AppProps } from '@/src/system/types';

const TranslatorApp: React.FC<AppProps> = ({ appId, systemServices, initialIntent, onNavigate, isActive }) => {
  // Wrapper for translator mode - actual rendering managed by parent during transition
  return null;
};

export default TranslatorApp;
