import React from 'react';
import type { AppProps } from '@/src/system/types';

const QuizApp: React.FC<AppProps> = ({ appId, systemServices, initialIntent, onNavigate, isActive }) => {
  // Wrapper for quiz mode - actual rendering managed by parent during transition
  return null;
};

export default QuizApp;
