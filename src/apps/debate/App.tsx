import React from 'react';
import type { AppProps } from '@/src/system/types';

const DebateApp: React.FC<AppProps> = ({ appId, systemServices, initialIntent, onNavigate, isActive }) => {
  // Wrapper for debate mode - actual rendering managed by parent during transition
  return null;
};

export default DebateApp;
