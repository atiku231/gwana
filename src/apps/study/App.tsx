import React from 'react';
import type { AppProps } from '@/src/system/types';

const StudyApp: React.FC<AppProps> = ({ appId, systemServices, initialIntent, onNavigate, isActive }) => {
  // Wrapper for study mode - actual rendering managed by parent during transition
  return null;
};

export default StudyApp;
