import React from 'react';
import type { AppProps } from '@/src/system/types';

const NewsApp: React.FC<AppProps> = ({ appId, systemServices, initialIntent, onNavigate, isActive }) => {
  // Wrapper for news mode - actual rendering managed by parent during transition
  return null;
};

export default NewsApp;
